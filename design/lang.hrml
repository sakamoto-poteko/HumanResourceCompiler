init floor[1] = 1;
init floor[0] = 0;

function inc(a) {
    return ++a;
}

sub start() {
    while (true) {
        let a = inbox();
        let b = inbox();
        let c = a * b;
        outbox(inc(c));
    }
}
