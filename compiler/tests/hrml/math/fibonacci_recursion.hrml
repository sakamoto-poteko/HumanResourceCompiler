// Fibonacci sequence
function fibonacci(n) {
    if (n == 0) {
        return 0;
    }
    if (n == 1) {
        return 1;
    }
    
    return fibonacci(n - 1) + fibonacci(n - 2);
}

sub start() {
    let number = inbox();
    let fib = fibonacci(number);
    outbox(fib);
}

// Input: 16
// Output: 987